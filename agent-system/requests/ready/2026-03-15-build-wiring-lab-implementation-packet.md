Status: ready
Priority: high
Date: 2026-03-15
Project: ohmic-audio-labs

# Build Wiring Lab Implementation Packet

## Goal

Turn the safe amp-match rules into a concrete implementation packet for the next
smart `Wiring Lab` version.

## Why

The rules note is finished. The next useful move is to make it implementable
without improvising in the UI layer.

## Deliverable

An implementation packet that defines:

- exact UI inputs
- exact result classes
- explanation blocks
- first supported amplifier target presets
- what is explicitly out of scope for v1

## Constraints

- no major UI build
- packet only

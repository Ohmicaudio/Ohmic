Status: planning_note
Date: 2026-03-15
Project: ohmic

# Ohmic Next Copy Cleanup Wave

## Why This Wave Exists

The public landing page now reflects the newer naming model, but adjacent
surfaces still carry older wording:

- static asset metadata still centers `AmpLab` where the copy should speak more
  generically about the Live Link or the broader shell
- engineering-diagram captions still mention placeholder AmpLab/tool wording
- the app shell still has a few harmonization leftovers, but some of those
  files are currently claimed by another lane

## Next Safe Parallel Tasks

1. static asset metadata and image catalog cleanup
2. static engineering-diagram caption cleanup
3. app-shell copy harmonization after the current worker claim releases

## Principle

Keep the naming model consistent without pretending every product/device term
must disappear. The goal is to keep:

- `Ohmic Live Link` generic
- `Ohmic Audio Labs` shell-level
- deck names surface-level
- device names device-specific
